# =============================================================
# Wrangler Configuration — TukTuk Social Thailand
# Phase 1: Cloudflare Pages (Hosting) ✅ DONE
# Phase 2: Cloudflare Workers (API)
# =============================================================

# ── CLOUDFLARE PAGES (Static Hosting) ─────────────────────────
name = "tuktukfeed"
pages_build_output_dir = "public"
compatibility_date = "2024-09-23"

# ── CLOUDFLARE WORKERS (API / Functions) ──────────────────────
# Workers config สำหรับ Phase 2
# cd workers && npm install && npx wrangler@3 deploy
# (run wrangler commands from the workers/ folder, not here)
